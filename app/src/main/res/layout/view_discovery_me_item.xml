<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:gravity="center_vertical"
    android:clickable="true"
    android:background="@drawable/bg_comm_item_selector"
    android:layout_height="@dimen/y50">

    <ImageView
        android:id="@+id/discovery_me_item_img_icon"
        android:layout_width="@dimen/x40"
        android:layout_height="@dimen/y40"
        android:layout_marginLeft="@dimen/x10"
        android:background="@drawable/head"
        android:layout_centerVertical="true"
        android:scaleType="centerCrop"
        android:visibility="visible"/>

    <TextView
        android:layout_toRightOf="@+id/discovery_me_item_img_icon"
        android:id="@+id/discovery_me_item_textview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:layout_centerVertical="true"
        android:text="@string/app_name"
        android:paddingLeft="15dp"
        android:singleLine="true"
        android:textSize="17dp"/>

    <ImageView
        android:id="@+id/discovery_me_item_img_ope"
        android:layout_alignParentRight="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="@dimen/x10"
        android:background="@drawable/right"
        android:layout_centerVertical="true"
        android:visibility="visible"/>
</RelativeLayout>